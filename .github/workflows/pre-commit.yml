# .github/workflows/pre-commit.yml
name: Pre-commit

on:
  push:
    branches: [dev, exercices-2.1-2.2-et-tp1, TP2]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
      - uses: pre-commit/action@v3.0.1
